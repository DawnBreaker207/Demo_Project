import { CheckTagsPipe } from './check-tags.pipe';

describe('CheckTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
